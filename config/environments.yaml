# ARTIFACT RESPONSIBILITY START (guidance, not project state)
# Created by: The initializer generates an unconfigured contract from the public-safe template; an authorized agent records owner-approved environments before remote work.
# Maintained / decided by: The deployment agent or authorized operator maintains verified connection and deployment settings; initialization never renews credential approval.
# Governing instructions: Bootstrap and remote-operation environment contract
# ARTIFACT RESPONSIBILITY END
# Public-safe starting point. No remote operation is authorized by this file.
# Template output: config/environments.yaml
schema_version: '1.0.0'
default_environment: null
environments: {}
